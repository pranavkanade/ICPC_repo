#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;

// manual length calc gives 1 more than the actual length
int main(){
  char str[] = "pranav";
  lli len = sizeof(str)/sizeof(char);
  cout << len << endl << endl;
  return 0;
}
